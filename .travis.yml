language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-aster' ]; then cd .. && eval "mv $currentfolder generator-aster" && cd generator-aster; fi

